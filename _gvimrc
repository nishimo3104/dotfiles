set imdisable
colorscheme railscasts
set showtabline=2
" window size
if has("win32")
  au GUIEnter * simalt ~x
else
  set transparency=8
  au GUIEnter * set lines=60
  au GUIEnter * set columns=190
endif
